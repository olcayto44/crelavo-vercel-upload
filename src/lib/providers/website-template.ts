import type { WebsiteGeneratedFile, WebsiteGenerationInput, WebsiteGenerationResult, WebsiteScope } from "./openai";

type WebsiteCopy = {
  title: string;
  eyebrow: string;
  headline: string;
  intro: string;
  features: Array<{ title: string; body: string }>;
  testimonials: Array<{ quote: string; name: string; role: string }>;
  faq: Array<{ question: string; answer: string }>;
};

function clean(value: unknown, fallback: string, limit = 240) {
  const text = String(value ?? "").replace(/[\u0000-\u001f\u007f]/g, " ").replace(/\s+/g, " ").trim();
  return (text || fallback).slice(0, limit);
}

export function escapeHtml(value: unknown) {
  return String(value ?? "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#39;");
}

function briefCopy(input: WebsiteGenerationInput): WebsiteCopy {
  const title = clean(input.brand, "Your next product");
  const audience = clean(input.audience, "ambitious teams", 120);
  const brief = clean(input.brief, `A focused digital experience for ${audience}.`, 360);
  const requestedFeatures = input.features.map((feature) => clean(feature, "Product workflow", 90)).filter(Boolean).slice(0, 3);
  const featureTitles = [...requestedFeatures, "Clear product direction", "Responsive by default", "A focused path to action"].slice(0, 3);
  return {
    title,
    eyebrow: clean(input.siteType, "Digital product experience", 100),
    headline: `${title} makes the next step feel simple.`,
    intro: brief,
    features: featureTitles.map((feature, index) => ({
      title: feature,
      body: index === 0 ? `A focused way for ${audience} to understand the offer and move forward with confidence.` : index === 1 ? "A polished foundation that keeps the experience useful across screens, teams and moments." : "A clear structure for explaining value, showing the workflow and inviting a meaningful conversation."
    })),
    testimonials: [
      { quote: "The structure makes our offer easier to understand without adding noise.", name: "Customer voice", role: "Product team" },
      { quote: "A calm, focused experience gives every important message room to work.", name: "Customer voice", role: "Growth team" }
    ],
    faq: [
      { question: "What is included in this experience?", answer: `A premium static website structure shaped around ${title}, its audience and the supplied brief.` },
      { question: "Can the content be refined later?", answer: "Yes. The delivered source keeps the content in readable static files so verified brand copy can be reviewed before publishing." },
      { question: "How do I start a project conversation?", answer: "Use the contact form or choose a plan to send the brief to the next step." }
    ]
  };
}

async function aiCopy(input: WebsiteGenerationInput, fallback: WebsiteCopy): Promise<WebsiteCopy> {
  const apiKey = String(process.env.OPENAI_API_KEY ?? "").trim();
  if (!apiKey) return fallback;
  try {
    const response = await fetch("https://api.openai.com/v1/chat/completions", {
      method: "POST",
      headers: { Authorization: `Bearer ${apiKey}`, "Content-Type": "application/json" },
      body: JSON.stringify({
        model: process.env.OPENAI_WEBSITE_MODEL || process.env.OPENAI_ASSISTANT_MODEL || "gpt-4o-mini",
        response_format: { type: "json_object" },
        temperature: 0.2,
        messages: [
          { role: "system", content: "Return only structured JSON copy for a premium static SaaS website. Never return HTML, CSS or JavaScript. Use supplied facts only. Never invent metrics, customer identities or claims. Keys: title, eyebrow, headline, intro, features (three title/body objects), testimonials (two quote/name/role objects with generic names if no names supplied), faq (three question/answer objects)." },
          { role: "user", content: JSON.stringify({ brief: input.brief, brand: input.brand, siteType: input.siteType, audience: input.audience, pages: input.pages, features: input.features, style: input.style }) }
        ]
      })
    });
    if (!response.ok) return fallback;
    const payload = await response.json();
    const raw = JSON.parse(String(payload.choices?.[0]?.message?.content ?? "{}")) as Partial<WebsiteCopy>;
    const features = Array.isArray(raw.features) ? raw.features.slice(0, 3).map((item) => ({ title: clean(item?.title, "Product capability", 100), body: clean(item?.body, "A clear capability shaped around the supplied brief.", 260) })) : fallback.features;
    const testimonials = Array.isArray(raw.testimonials) ? raw.testimonials.slice(0, 2).map((item) => ({ quote: clean(item?.quote, "A clearer way to move forward.", 240), name: clean(item?.name, "Customer voice", 80), role: clean(item?.role, "Product team", 80) })) : fallback.testimonials;
    const faq = Array.isArray(raw.faq) ? raw.faq.slice(0, 3).map((item) => ({ question: clean(item?.question, "How does it work?", 140), answer: clean(item?.answer, "The supplied brief is turned into a focused, reviewable website experience.", 260) })) : fallback.faq;
    return { title: clean(raw.title, fallback.title), eyebrow: clean(raw.eyebrow, fallback.eyebrow), headline: clean(raw.headline, fallback.headline, 180), intro: clean(raw.intro, fallback.intro, 360), features: features.length === 3 ? features : fallback.features, testimonials: testimonials.length === 2 ? testimonials : fallback.testimonials, faq: faq.length >= 2 ? faq : fallback.faq };
  } catch {
    return fallback;
  }
}

function html(input: WebsiteCopy) {
  const featureMarkup = input.features.map((feature, index) => `<article class="feature-card glass-card"><span class="feature-index">0${index + 1}</span><h3>${escapeHtml(feature.title)}</h3><p>${escapeHtml(feature.body)}</p></article>`).join("");
  const testimonialMarkup = input.testimonials.map((item) => `<article class="testimonial-item glass-card"><p>“${escapeHtml(item.quote)}”</p><strong>${escapeHtml(item.name)}</strong><span>${escapeHtml(item.role)}</span></article>`).join("");
  const faqMarkup = input.faq.map((item, index) => `<div class="faq-item"><button class="faq-trigger" type="button" aria-expanded="${index === 0 ? "true" : "false"}" aria-controls="faq-panel-${index}">${escapeHtml(item.question)}<span aria-hidden="true">+</span></button><div id="faq-panel-${index}" class="faq-panel" ${index === 0 ? "" : "hidden"}><p>${escapeHtml(item.answer)}</p></div></div>`).join("");
  return `<!doctype html>
<html lang="en">
<head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="description" content="${escapeHtml(input.intro)}"><title>${escapeHtml(input.title)}</title><link rel="stylesheet" href="./styles.css"></head>
<body>
<header class="site-header"><a class="brand" href="#hero">${escapeHtml(input.title)}</a><nav aria-label="Primary navigation"><a href="#features">Capabilities</a><a href="#workflow">Workflow</a><a href="#pricing">Plans</a><a href="#contact">Contact</a></nav><a class="nav-cta" href="#contact">Start a conversation</a></header>
<main>
<section id="hero" class="hero section-shell"><div class="hero-copy"><p class="eyebrow">${escapeHtml(input.eyebrow)}</p><h1>${escapeHtml(input.headline)}</h1><p class="hero-intro">${escapeHtml(input.intro)}</p><div class="hero-actions"><a class="cta primary-cta" href="#pricing">Explore plans</a><a class="cta secondary-cta" href="#contact">Talk to the team</a></div><div class="hero-note"><span class="status-dot"></span><span>Designed for clarity, built for momentum</span></div></div><div class="hero-dashboard glass-card" aria-label="Product dashboard preview"><div class="dashboard-top"><span class="window-dots"><i></i><i></i><i></i></span><span>Workspace overview</span><span class="live-pill">Live view</span></div><div class="dashboard-layout"><aside class="dashboard-sidebar"><span class="side-mark"></span><span class="side-line active"></span><span class="side-line"></span><span class="side-line"></span><span class="side-line"></span></aside><div class="dashboard-content"><div class="dashboard-heading"><div><span class="mini-label">Today</span><strong>Focused progress</strong></div><span class="avatar">CL</span></div><div class="dashboard-panels"><div class="dashboard-panel panel-large"><span class="mini-label">Momentum</span><strong>Clear next steps</strong><div class="sparkline"><svg viewBox="0 0 240 70" role="img" aria-label="Progress trend"><path d="M4 58 C28 53 28 42 54 46 S82 26 108 35 S141 18 164 25 S193 8 236 12" fill="none" stroke="currentColor" stroke-width="4" stroke-linecap="round"/><circle cx="236" cy="12" r="5" fill="currentColor"/></svg></div></div><div class="dashboard-panel"><span class="mini-label">Active flow</span><strong>In review</strong><span class="panel-bar"><i></i></span></div><div class="dashboard-panel"><span class="mini-label">Team focus</span><strong>Aligned</strong><span class="panel-check">✓</span></div></div><div class="dashboard-footer"><span>Brief</span><span>Structure</span><span>Review</span><span class="footer-progress"><i></i></span></div></div></div></div></div></section>
<section id="about" class="about section-shell split-section"><div><p class="eyebrow">About the approach</p><h2>Make the important message impossible to miss.</h2></div><div><p>Every surface is shaped around the supplied brief, the people it serves and the action that matters next. The result is a focused foundation that can be reviewed, refined and published with confidence.</p><a class="text-link" href="#contact">Bring your brief to life <span>→</span></a></div></section>
<section id="features" class="section-shell"><div class="section-heading"><p class="eyebrow">Capabilities</p><h2>Everything the first impression needs.</h2><p>A considered structure for explaining value without making the visitor work for it.</p></div><div class="feature-grid">${featureMarkup}</div></section>
<section id="workflow" class="section-shell workflow-section"><div class="section-heading"><p class="eyebrow">The workflow</p><h2>From first brief to confident launch.</h2></div><div class="workflow-timeline"><article class="workflow-step"><span>01</span><h3>Clarify</h3><p>Turn the brief into a clear promise and audience-led direction.</p></article><article class="workflow-step"><span>02</span><h3>Shape</h3><p>Organize the story into useful sections, proof and next steps.</p></article><article class="workflow-step"><span>03</span><h3>Review</h3><p>Refine the words and details before anything is published.</p></article><article class="workflow-step"><span>04</span><h3>Launch</h3><p>Receive a responsive source package ready for the next handoff.</p></article></div></section>
<section id="pricing" class="section-shell"><div class="section-heading"><p class="eyebrow">Plans</p><h2>Choose the pace that fits your next move.</h2><p>Start with the level of support that matches the shape of your brief.</p></div><div class="pricing-grid"><article class="pricing-card glass-card"><span class="plan-label">Foundation</span><h3>Starter</h3><p>For a focused first launch and a clear digital home.</p><ul><li>Core page structure</li><li>Responsive layout</li><li>Review-ready source</li></ul><button class="plan-button" type="button" data-plan="starter">Choose Plan</button></article><article class="pricing-card glass-card featured-plan"><span class="plan-label">Most complete</span><h3>Growth</h3><p>For teams turning a strong offer into a repeatable experience.</p><ul><li>Expanded capability story</li><li>Conversion-led sections</li><li>Refinement conversation</li></ul><button class="plan-button" type="button" data-plan="growth">Choose Plan</button></article><article class="pricing-card glass-card"><span class="plan-label">Tailored</span><h3>Scale</h3><p>For a broader launch that needs a considered production path.</p><ul><li>Custom project direction</li><li>Admin-ready handoff option</li><li>Launch planning support</li></ul><button class="plan-button" type="button" data-plan="scale">Choose Plan</button></article></div></section>
<section id="testimonials" class="section-shell"><div class="section-heading"><p class="eyebrow">Customer perspective</p><h2>Clarity compounds.</h2></div><div class="testimonial-grid">${testimonialMarkup}</div></section>
<section id="faq" class="section-shell faq-section"><div class="section-heading"><p class="eyebrow">Questions</p><h2>Good work starts with clear answers.</h2></div><div class="faq-list">${faqMarkup}</div></section>
<section id="contact" class="section-shell contact-section"><div><p class="eyebrow">Start here</p><h2>Ready to make the next step clearer?</h2><p>Share a little context and the team can take it from there.</p></div><form class="contact-form" action="#contact" method="post"><label for="name">Name</label><input id="name" name="name" autocomplete="name" required><label for="email">Work email</label><input id="email" name="email" type="email" autocomplete="email" required><label for="message">What are you building?</label><textarea id="message" name="message" rows="4" required></textarea><button class="cta primary-cta" type="submit">Send inquiry</button><p class="form-note">Your message stays in this demo until a secure form endpoint is connected.</p></form></section>
</main><footer class="site-footer section-shell"><span>${escapeHtml(input.title)}</span><a href="#hero">Back to top ↑</a></footer><div id="plan-modal" class="plan-modal" hidden role="dialog" aria-modal="true" aria-labelledby="plan-modal-title"><div class="modal-card glass-card"><button class="modal-close" type="button" aria-label="Close">×</button><p class="eyebrow">Plan selected</p><h2 id="plan-modal-title">Let’s shape the right next step.</h2><p id="plan-modal-copy">Tell us what you need and we’ll continue from your brief.</p><a class="cta primary-cta" href="#contact">Continue to contact</a></div></div><script src="./script.js"></script>
</body></html>`;
}

const styles = `:root{color-scheme:dark;--bg:#070b18;--surface:rgba(17,25,50,0.72);--line:rgba(255,255,255,0.14);--muted:#9caac5;--text:#f7f9ff;--cyan:#57e5ff;--violet:#8468ff;--gradient:linear-gradient(135deg,#50e7ff 0%,#8468ff 100%);font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;background:radial-gradient(circle at 12% 0%,rgba(38,102,184,0.28),transparent 32rem),radial-gradient(circle at 90% 8%,rgba(111,66,202,0.26),transparent 35rem),linear-gradient(145deg,#070b18 0%,#0b1124 58%,#080b18 100%);color:var(--text);line-height:1.6}body:before{content:"";position:fixed;inset:0;pointer-events:none;background-image:linear-gradient(rgba(255,255,255,.025) 1px,transparent 1px),linear-gradient(90deg,rgba(255,255,255,.025) 1px,transparent 1px);background-size:72px 72px;mask-image:linear-gradient(to bottom,black,transparent 70%)}a{color:inherit;text-decoration:none}button,input,textarea{font:inherit}button{cursor:pointer}.site-header,.section-shell,.site-footer{width:min(1160px,calc(100% - 40px));margin-inline:auto}.site-header{display:flex;align-items:center;gap:28px;padding:28px 0}.site-header nav{display:flex;gap:22px;margin-left:auto;color:var(--muted);font-size:.9rem}.site-header nav a:hover,.text-link:hover{color:var(--cyan)}.brand{font-weight:800;letter-spacing:-.03em}.nav-cta,.cta{display:inline-flex;align-items:center;justify-content:center;border-radius:999px;padding:.75rem 1.15rem;font-weight:750;transition:transform .2s ease,filter .2s ease,border-color .2s ease}.nav-cta,.secondary-cta{border:1px solid var(--line);color:#dce7ff}.nav-cta:hover,.secondary-cta:hover{border-color:rgba(87,229,255,.55);transform:translateY(-2px)}.hero{display:grid;grid-template-columns:minmax(0,.94fr) minmax(420px,1.06fr);gap:48px;align-items:center;padding:clamp(48px,8vw,104px) 0 88px}.eyebrow,.mini-label,.plan-label{color:var(--cyan);font-size:.73rem;font-weight:800;letter-spacing:.16em;text-transform:uppercase}.hero h1{max-width:650px;margin:18px 0;font-size:clamp(3.2rem,7vw,6.5rem);line-height:.94;letter-spacing:-.075em}.hero-intro{max-width:580px;color:var(--muted);font-size:1.1rem}.hero-actions{display:flex;gap:12px;flex-wrap:wrap;margin-top:28px}.primary-cta{background:var(--gradient);color:white;border:0}.primary-cta:hover{transform:translateY(-3px);filter:brightness(1.12)}.hero-note{display:flex;align-items:center;gap:9px;color:#b5c3db;font-size:.82rem;margin-top:28px}.status-dot{width:8px;height:8px;border-radius:50%;background:#67efb1;box-shadow:0 0 16px #67efb1}.glass-card{background:var(--surface);backdrop-filter:blur(20px);-webkit-backdrop-filter:blur(20px);border:1px solid var(--line);box-shadow:0 28px 90px rgba(0,0,0,.3)}.hero-dashboard{border-radius:26px;padding:18px;transform:rotate(1.5deg);transition:transform .3s ease}.hero-dashboard:hover{transform:rotate(0) translateY(-5px)}.dashboard-top,.dashboard-heading,.dashboard-footer{display:flex;align-items:center;gap:12px;color:#b3bfd5;font-size:.72rem}.dashboard-top{padding:3px 5px 18px;border-bottom:1px solid var(--line)}.window-dots{display:flex;gap:5px;margin-right:auto}.window-dots i{width:7px;height:7px;border-radius:50%;background:#7787a5}.live-pill{color:#9bf3d0;background:rgba(72,215,160,.1);padding:4px 8px;border-radius:999px}.dashboard-layout{display:grid;grid-template-columns:54px 1fr;min-height:305px}.dashboard-sidebar{border-right:1px solid var(--line);padding:18px 12px;display:grid;align-content:start;gap:17px}.side-mark{width:28px;height:28px;border-radius:9px;background:var(--gradient);margin-bottom:18px}.side-line{height:7px;border-radius:9px;background:#586783}.side-line.active{background:var(--cyan)}.dashboard-content{padding:22px 10px 8px 22px}.dashboard-heading strong{display:block;color:var(--text);font-size:1.05rem;margin-top:4px}.avatar{display:grid;place-items:center;width:31px;height:31px;border-radius:50%;background:#233155;color:#c8d5ff;margin-left:auto}.dashboard-panels{display:grid;grid-template-columns:1.4fr 1fr;gap:10px;margin-top:24px}.dashboard-panel{position:relative;min-height:108px;padding:15px;border:1px solid var(--line);border-radius:15px;background:rgba(6,12,29,.48)}.dashboard-panel strong{display:block;font-size:1.05rem;margin-top:8px}.panel-large{grid-row:span 2;min-height:226px}.sparkline{color:var(--cyan);margin-top:46px}.sparkline svg{width:100%;height:auto}.panel-bar{display:block;height:6px;background:#273354;border-radius:8px;margin-top:20px}.panel-bar i{display:block;width:72%;height:100%;border-radius:8px;background:var(--gradient)}.panel-check{display:block;color:#76f2c3;font-size:1.8rem;margin-top:14px}.dashboard-footer{padding-top:18px}.dashboard-footer span{color:#7787a5}.footer-progress{height:4px;flex:1;background:#273354;border-radius:8px;margin-left:auto}.footer-progress i{display:block;width:67%;height:100%;background:var(--gradient);border-radius:8px}.split-section{display:grid;grid-template-columns:1fr 1fr;gap:70px;padding:90px 0;border-top:1px solid var(--line)}h2{font-size:clamp(2rem,4vw,3.8rem);line-height:1.02;letter-spacing:-.06em;margin:10px 0 20px}h3{margin:8px 0;font-size:1.2rem}p{color:var(--muted);margin:0 0 16px}.text-link{color:#dfe8ff;font-weight:750}.text-link span{color:var(--cyan);margin-left:8px}.section-heading{max-width:690px;margin-bottom:34px}.section-heading>p:last-child{max-width:560px}.feature-grid,.pricing-grid,.testimonial-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:16px}.feature-card,.pricing-card,.testimonial-item{border-radius:20px;padding:24px;transition:transform .2s ease,border-color .2s ease,box-shadow .2s ease}.feature-card:hover,.pricing-card:hover,.testimonial-item:hover,.workflow-step:hover{transform:translateY(-5px);border-color:rgba(87,229,255,.45);box-shadow:0 28px 70px rgba(20,73,135,.25)}.feature-index{color:#667798;font-size:.76rem}.workflow-section{padding:96px 0}.workflow-timeline{display:grid;grid-template-columns:repeat(4,1fr);gap:14px}.workflow-step{padding:24px;border-top:1px solid rgba(87,229,255,.6);transition:transform .2s ease}.workflow-step>span{color:var(--cyan);font-weight:800}.pricing-card{position:relative}.featured-plan{background:linear-gradient(160deg,rgba(57,80,174,.55),rgba(17,25,50,.76));border-color:rgba(132,104,255,.65)}.plan-label{font-size:.68rem;color:#a7b5d0}.pricing-card h3{font-size:1.7rem;margin-top:16px}.pricing-card ul{list-style:none;padding:0;margin:24px 0;display:grid;gap:10px;color:#c2cee2}.pricing-card li:before{content:"✓";color:var(--cyan);margin-right:9px}.plan-button{width:100%;border:1px solid rgba(255,255,255,.17);background:rgba(255,255,255,.06);color:#f5f7ff;border-radius:999px;padding:.8rem;font-weight:750}.featured-plan .plan-button{background:var(--gradient);border-color:transparent}.plan-button:hover{filter:brightness(1.12);transform:translateY(-2px)}.testimonial-grid{grid-template-columns:repeat(2,1fr)}.testimonial-item p{color:#dce5f6;font-size:1.12rem}.testimonial-item strong,.testimonial-item span{display:block}.testimonial-item span{color:#7382a0;font-size:.82rem;margin-top:3px}.faq-section{padding:96px 0}.faq-list{max-width:820px}.faq-item{border-top:1px solid var(--line)}.faq-trigger{display:flex;justify-content:space-between;width:100%;padding:20px 0;background:none;border:0;color:var(--text);text-align:left;font-weight:750}.faq-trigger span{color:var(--cyan);font-size:1.5rem;font-weight:400}.faq-panel{padding:0 50px 18px 0}.contact-section{display:grid;grid-template-columns:1fr 1fr;gap:60px;padding:76px 0;border-top:1px solid var(--line)}.contact-form{display:grid;gap:8px}.contact-form label{font-size:.82rem;color:#c5d0e4;margin-top:9px}.contact-form input,.contact-form textarea{width:100%;border:1px solid var(--line);border-radius:12px;background:rgba(255,255,255,.055);color:var(--text);padding:.8rem;outline:none}.contact-form input:focus,.contact-form textarea:focus{border-color:var(--cyan);box-shadow:0 0 0 3px rgba(87,229,255,.12)}.form-note{font-size:.75rem;margin-top:8px}.site-footer{border-top:1px solid var(--line);padding:26px 0 40px;display:flex;justify-content:space-between;color:#8897b4;font-size:.82rem}.plan-modal{position:fixed;inset:0;display:grid;place-items:center;padding:20px;background:rgba(2,5,14,.72);z-index:10}.plan-modal[hidden]{display:none}.modal-card{position:relative;max-width:500px;border-radius:22px;padding:34px}.modal-close{position:absolute;right:16px;top:12px;background:none;border:0;color:#b8c5dc;font-size:1.7rem}.modal-close:hover{color:white}@media (max-width:860px){.site-header nav{display:none}.hero,.split-section,.contact-section{grid-template-columns:1fr}.hero-dashboard{transform:none}.feature-grid,.pricing-grid,.workflow-timeline{grid-template-columns:repeat(2,1fr)}}@media (max-width:560px){.site-header,.section-shell,.site-footer{width:min(100% - 28px,1160px)}.site-header{padding:20px 0}.nav-cta{font-size:.78rem}.hero{padding-top:50px}.hero h1{font-size:3.3rem}.dashboard-layout{grid-template-columns:38px 1fr}.dashboard-sidebar{padding-inline:7px}.dashboard-content{padding-left:12px}.dashboard-panels,.feature-grid,.pricing-grid,.workflow-timeline,.testimonial-grid{grid-template-columns:1fr}.panel-large{grid-row:auto}.split-section,.workflow-section,.faq-section{padding:64px 0}.contact-section{padding:54px 0}.site-footer{padding-bottom:28px}}
`;

const script = `document.querySelectorAll('a[href^="#"]').forEach((link) => link.addEventListener('click', (event) => { const target = document.querySelector(link.getAttribute('href')); if (target) { event.preventDefault(); target.scrollIntoView({ behavior: 'smooth', block: 'start' }); } })); document.querySelectorAll('.faq-trigger').forEach((button) => button.addEventListener('click', () => { const expanded = button.getAttribute('aria-expanded') === 'true'; button.setAttribute('aria-expanded', String(!expanded)); const panel = document.getElementById(button.getAttribute('aria-controls') || ''); if (panel) panel.hidden = expanded; })); const modal = document.getElementById('plan-modal'); const modalCopy = document.getElementById('plan-modal-copy'); document.querySelectorAll('[data-plan]').forEach((button) => button.addEventListener('click', () => { if (!modal) return; modal.hidden = false; if (modalCopy) modalCopy.textContent = button.getAttribute('data-plan') + ' plan selected. Tell us what you need and we will continue from your brief.'; })); document.querySelector('.modal-close')?.addEventListener('click', () => { if (modal) modal.hidden = true; }); modal?.addEventListener('click', (event) => { if (event.target === modal) modal.hidden = true; }); document.querySelector('.contact-form')?.addEventListener('submit', (event) => { event.preventDefault(); const note = document.querySelector('.form-note'); if (note) note.textContent = 'Thanks. Your inquiry is ready for a secure endpoint.'; });`;

function adminFiles(title: string): WebsiteGeneratedFile[] {
  const safeTitle = escapeHtml(title);
  return [
    { path: "admin/index.html", contentType: "text/html", content: `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${safeTitle} Admin</title><link rel="stylesheet" href="./styles.css"></head><body><main><p class="eyebrow">Admin starter</p><h1>${safeTitle} Admin</h1><p>This starter uses demo auth only. Configure production authentication, authorization and server-side sessions before deployment.</p><div class="admin-grid"><article><h2>Content</h2><p>Review published sections and changes.</p></article><article><h2>Leads</h2><p>Review contact submissions after connecting a secure backend.</p></article><article><h2>Analytics</h2><p>Connect an approved analytics provider before production.</p></article></div><p class="notice">Demo auth only. Keep secrets on the server and configure production auth before deployment.</p></main><script src="./script.js"></script></body></html>` },
    { path: "admin/styles.css", contentType: "text/css", content: `:root{font-family:Inter,system-ui,sans-serif;color:#f8fbff;background:#080d1d}body{margin:0;background:radial-gradient(circle at 15% 0%,rgba(49,88,170,.3),transparent 30rem)}main{width:min(1080px,calc(100% - 2rem));margin:auto;padding:4rem 0}.admin-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:1rem}.admin-grid article,.notice{padding:1.5rem;border:1px solid rgba(255,255,255,.16);border-radius:1rem;background:rgba(22,33,60,.8);box-shadow:0 18px 45px rgba(0,0,0,.3)}.eyebrow{color:#7dd3fc;font-weight:800;text-transform:uppercase;letter-spacing:.12em}.notice{margin-top:1rem;color:#fde68a}@media(max-width:760px){.admin-grid{grid-template-columns:1fr}}` },
    { path: "admin/script.js", contentType: "application/javascript", content: `document.querySelectorAll('[data-demo-auth]').forEach((element) => element.addEventListener('click', () => document.dispatchEvent(new CustomEvent('demo-auth-request'))));` },
    { path: ".env.example", contentType: "text/markdown", content: "PUBLIC_SITE_URL=http://localhost:3000\nADMIN_AUTH_PROVIDER=replace-with-production-provider\n" },
    { path: "data/schema.json", contentType: "application/json", content: JSON.stringify({ version: 1, entities: [{ name: "leads", fields: ["id", "name", "email", "message", "created_at"] }, { name: "content", fields: ["id", "section_id", "title", "body", "updated_at"] }], auth: { mode: "demo", productionNote: "Configure server-side authentication and authorization before deployment." } }, null, 2) }
  ];
}

export async function generateDeterministicWebsiteSource(input: WebsiteGenerationInput): Promise<WebsiteGenerationResult> {
  const fallback = briefCopy(input);
  const copy = await aiCopy(input, fallback);
  const files: WebsiteGeneratedFile[] = [
    { path: "index.html", content: html(copy), contentType: "text/html" },
    { path: "styles.css", content: styles, contentType: "text/css" },
    { path: "script.js", content: script, contentType: "application/javascript" },
    { path: "README.md", content: `# ${copy.title}\n\nThis package is a deterministic premium static website generated from the supplied brief. Review copy, connect a secure contact endpoint and replace verified brand details before publishing.`, contentType: "text/markdown" }
  ];
  if ((input.scope ?? "marketing_website") === "website_with_admin") files.push(...adminFiles(copy.title));
  return { siteTitle: copy.title, framework: "static-html", files };
}

export type { WebsiteScope };
